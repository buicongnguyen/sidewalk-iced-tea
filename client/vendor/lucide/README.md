# Lucide icons

Selected, unmodified SVGs from `lucide-static@1.44.0` (https://lucide.dev).
The upstream ISC license is included in `LICENSE`. These icons are vendored so
settings and game controls remain available offline, without a CDN dependency.
